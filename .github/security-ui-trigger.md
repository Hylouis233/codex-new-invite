Trigger the one-shot same-repository workflow that sanitizes dynamic usage-page rendering and removes CPA management-key persistence.

Retrigger after the workflow was registered on the default branch.
